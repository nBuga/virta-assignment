{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="d-inline me-3">{{ title }}</h1> <i class="fas fa-edit"></i>
        <div class="row mt-5">
            <div class="col-12 col-md-8 ps-5">
                    <div class="d-flex mb-3" {{ stimulus_controller('station-controls') }}>
                    </div> <br />
                </div>
            </div>
        </div>
    </div>
{% endblock %}
